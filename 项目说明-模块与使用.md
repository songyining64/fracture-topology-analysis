# 断裂拓扑分析项目 — 模块说明与使用指南

本文档分两部分：**一、envs 环境是什么**；**二、niuyuxi 每个模块的用途与使用方法**。

---

## 一、envs：第一个「环境」是什么

### 1.1 定义

`envs/` 是项目的 **Python 虚拟环境**，不是「一个文件」，而是一个**独立 Python 运行环境**的目录。  
里面安装了断裂拓扑分析所需的全部第三方库，你的脚本需要在**激活这个环境后**再运行，才能正确 `import fractopo` 等包。

### 1.2 结构概览

```
envs/
└── fract/                    # 虚拟环境名称（fract = fracture 缩写）
    ├── Scripts/               # Windows 下：python.exe、pip、activate 等
    │   └── (或 Linux/Mac 下为 bin/)
    ├── Lib/
    │   └── site-packages/     # 所有已安装的包都在这里
    │       ├── fractopo/      # 断裂网络分析核心库
    │       ├── geopandas/
    │       ├── PyQt5/
    │       ├── fatbox/        # 图像/骨架分析（new plot 用）
    │       ├── shapely, pandas, numpy, matplotlib, scipy, ...
    │       └── ...
    └── pyvenv.cfg             # 指向基础 Python 的配置（若存在）
```

- **fract**：环境名；实际可执行的是 `envs/fract/Scripts/python`（或 `bin/python`）。
- **Lib/site-packages**：你 `pip install` 的东西都在这里；**不要手动改这里的库代码**，除非你明确在调试 fractopo 本身。

### 1.3 与项目相关的核心包（envs 里）

| 包名 | 作用 |
|------|------|
| **fractopo** | 断裂迹线 + 研究区 → 分支/节点拓扑、参数、网格采样、各种图（玫瑰图、三元图、等值线等） |
| **geopandas** | 读写 GeoJSON/Shapefile，几何计算 |
| **shapely** | 点线面几何对象（LineString, Polygon 等） |
| **PyQt5** | main.py 的 GUI 界面 |
| **matplotlib** | 所有 Python 脚本里的出图 |
| **scipy** | 统计（如 KDE 热力图）、优化等 |
| **fatbox** | new plot 里图像预处理、骨架、图编辑、度量与绘图 |
| **ternary** | 三元图（XYI、分支类型占比） |
| **powerlaw** | 迹线/分支长度幂律拟合 |
| **pyshp / shapefile** | analyis.py 读写的 Shapefile |
| **skimage** | 图像滤波、骨架化等（new plot） |
| **opencv (cv2)** | 连通分量等（new plot） |
| **networkx** | 图结构（new plot 里骨架→图） |

### 1.4 如何使用「第一个环境」

在**运行 niuyuxi 里任何脚本之前**，先激活 `envs/fract`：

- **Windows（CMD）**  
  ```bat
  envs\fract\Scripts\activate
  ```
- **Windows（PowerShell）**  
  ```powershell
  envs\fract\Scripts\Activate.ps1
  ```
- **Mac / Linux**  
  ```bash
  source envs/fract/bin/activate
  ```

激活后终端前面会出现 `(fract)`，然后：

```bash
cd niuyuxi
python main.py
# 或
python "data export.py"
```

这样才会用 `envs/fract` 里的 Python 和 fractopo，不会报 `No module named 'fractopo'`。

---

## 二、niuyuxi：每个模块有什么用、怎么用

niuyuxi 是**第二个**你要关心的部分：**项目主目录**，里面是**你的数据、脚本和界面**。下面按「根目录脚本 → 子目录」逐项说明**每个模块的用途**和**具体使用方式**。

---

### 2.1 数据与配置（niuyuxi 根目录）

| 文件/夹 | 用途 | 使用说明 |
|--------|------|----------|
| **Yingmai 2 area in Tarim Basin.csv** | 网格化拓扑/几何统计结果 | 由 `data export.py` 生成；每行一个网格单元，含顶点坐标和各参数列；可导入 ArcGIS/QGIS 做等值线或进一步统计。 |
| **THK/**（需自备） | 当前默认数据目录 | 应包含 `thkceshi-landmark1.geojson`（迹线）和 `my_area.geojson`（研究区）。脚本里写的是相对路径，运行前请把工作目录切到 `niuyuxi`。 |
| **.idea/** | IDE 配置 | PyCharm/IntelliJ 项目配置；可忽略。 |
| **.cache/** | fractopo 缓存 | 网格采样、裁剪等中间结果；删掉会重算，不影响正确性。 |

---

### 2.2 根目录 Python 脚本 — 用途与用法

#### 1）`main.py` — 图形界面总入口（最常用）

- **作用**：启动 PyQt5 窗口，通过按钮和下拉框完成「原图 → 分类 → 拓扑 → 热力图/玫瑰图/三元图/等值线/参数表」等，无需改代码。
- **依赖**：`demo.py`（界面布局）、`THK/` 下 geojson、环境里 PyQt5 + fractopo。
- **怎么用**：
  1. 激活 `envs/fract`，`cd niuyuxi`。
  2. 运行：`python main.py`。
  3. 在界面中：
     - **原断裂数据地图**：只画迹线 + 研究区边界。
     - **分类后断裂数据地图**：按 CC/CI/II 分支和 X/Y/I 节点着色。
     - **拓扑化后 1/2**：在下拉框选「拓扑化后断裂数据地图1」或「2」，再点对应按钮。
     - **拓扑后各参数数据**：在右侧文本框显示 `network.parameters`。
     - **方位角集图**：按方位组（如 N-S、E-W）着色。
     - **断裂密度热力图**：KDE 密度 + 散点。
     - **分支与线条方向玫瑰图**：trace / branch 方位玫瑰图。
     - **节点与分支各类别占比三元图**：XYI 与分支类型三元图。
     - **交叉与相邻关系**：方位集的交切/对接关系图。
     - **确定分支和节点**：迹线 + 研究区 + 节点类型对比图。
     - **轮廓图**：在第一个下拉框选参数（断裂强度、长度、连接数等），再点一次「轮廓图」相关按钮（代码里通过 `comboBox` 的选项索引触发 `run_lunkuo`），用网格 1000 做等值线。
- **注意**：数据路径在 main.py 里写死为 THK，换数据需改 `trace_data_url`、`area_data_url` 和 `name`。

#### 2）`demo.py` — 界面布局（不要手改）

- **作用**：由 Qt 的 `6.ui` 用 pyuic5 生成的 Python 界面类 `Ui_MainWindow`；定义所有按钮、下拉框、文本框等。
- **怎么用**：仅被 `main.py` 引用；**不要直接运行**。改界面请改 `6.ui` 再用 pyuic5 重新生成。

#### 3）`data export.py` — 网格采样并导出 CSV

- **作用**：读 THK 的迹线 + 研究区 → 建 Network → `contour_grid(cell_width=750)` → 把每个网格的顶点和所有拓扑/几何指标导出为 CSV。
- **怎么用**：
  1. 确保 `THK/thkceshi-landmark1.geojson` 和 `THK/my_area.geojson` 存在。
  2. 在 `niuyuxi` 下运行：`python "data export.py"`。
  3. 得到 `Yingmai 2 area in Tarim Basin.csv`。若要换网格大小，改 `cell_width=750`；换数据改文件路径和 `name`。

#### 4）`分类后-拓扑后.py` — 单张「分类+拓扑」图

- **作用**：不弹界面，直接画「分支按类型着色 + 迹线 + 节点按类型着色 + 研究区边界」，保存为 `plot13.pdf`。
- **怎么用**：在 niuyuxi 下 `python 分类后-拓扑后.py`。数据路径在脚本里（THK），可改。

#### 5）`1.py` — 两网络拓扑参数对比

- **作用**：建两个 Network（KB11 与 MY），选部分参数（如 B22、Connections per Branch），用 `plot_parameters_plot` 画对比图，保存 `plot13.pdf`。
- **怎么用**：确保 `KB11/`、`THK/`（或 MY）下对应 geojson 存在，在 niuyuxi 下 `python 1.py`。要对比别的网络或参数，改路径和 `selected` 集合。

#### 6）`2.py` — 多网络迹线长度分布与幂律拟合

- **作用**：建 KB11、MY、THK 三个 Network，清 fractopo 缓存，做多网络迹线长度分布与 cut-off 优化、幂律拟合，出图保存 `plot14.pdf`。
- **怎么用**：同 1.py，数据目录要存在；运行 `python 2.py`。

#### 7）`3.py` — 拓扑验证错误示意图

- **作用**：画 6 张小图，示意 fractopo 会报的几何错误（多线共点、重叠、V 节点、多次交叉等），可保存 `validation_errors.png`。
- **怎么用**：`python 3.py`，主要用于文档或排查数据问题。

#### 8）`celltest.py` — 网格采样小测试

- **作用**：按 `data_option` 选 KB11/THK/MY，建 Network，做 `contour_grid(cell_width=1000)` 并打印边界、网格数等，用于确认网格设置。
- **怎么用**：改 `data_option`（1/2/3）和可选参数，在 niuyuxi 下 `python celltest.py`。

---

### 2.3 `new plot/` — 另一套分析与出图

这一块是「**从栅格图 + 骨架 + 图**」做网格统计和玫瑰图，和主流程（矢量化迹线 + fractopo）互补。

| 文件 | 用途 | 使用说明 |
|------|------|----------|
| **new plot.py** | 主脚本 | 读 geojson → 画图 → 导出 PNG → 二值化/骨架/连通分量 → 建 NetworkX 图 → 三分点分裂、边长、走向 → 用 FracAnalysisPoly + FancyPlot 画网格玫瑰图等。需同目录下有 `analyis.py`、`analyis_test.py` 及 fatbox（preprocessing/metrics/edits/plots）。**运行**：在 `new plot/` 目录下、激活 fract 环境后 `python "new plot.py"`；数据路径在脚本内（geojson + my_area），需自备。 |
| **analyis.py** | 网格+方位统计与玫瑰图 | 提供 `FracAnalysisPoly`（读 shapefile 折线，按网格+角度箱算 L、N、各向异性）、`FracAnalysisPoint`（点 shapefile）、`FancyPlot`/`FancyPlotTotals`（画玫瑰图、色块、圆圈等）。被 `new plot.py` 调用。 |
| **analyis_test.py** | GeoJSON→Shapefile | `geojson2shp(geojson_file, shp_file)`：把 geojson 转为 shp 供 analyis 读。可单独运行或由 new plot 调用。 |
| **preprocessing.py, metrics.py, edits.py, plots.py, utils.py** | 图像/图算法 | 来自 fatbox（或同目录拷贝），做阈值、骨架、连通分量、图编辑、边长/走向计算等。new plot 依赖这些。 |

使用顺序建议：若要跑通 `new plot.py`，先保证 `thkceshi-landmark1.geojson`、`my_area.geojson` 在 new plot 能访问的路径（或改脚本路径），再在 `new plot/` 下运行；若缺少 fatbox，需从 `envs/fract/Lib/site-packages/fatbox` 拷到 `new plot/` 或把 envs 的 site-packages 放在 Python 路径中。

---

### 2.4 `数据处理/` — 数据准备小工具

| 文件 | 用途 | 使用说明 |
|------|------|----------|
| **traces.py** | 文本坐标 → GeoJSON 迹线 | 读 `chemogulongqibei.dat`（空格分隔 x, y, type），按 type 连成线，写出 `thkceshi-landmark1.geojson`。改输入文件名和输出路径即可复用。 |
| **area.py** | 迹线范围 → 研究区 GeoJSON | 读 `thkceshi-landmark1.geojson`，用所有顶点算最小外接矩形，写出 `my_area.geojson`。适合「先有迹线、后框一个矩形研究区」。 |
| **边缘/bianyuan1.py, bianyuan2.py** | 边缘/边界处理 | 具体逻辑需打开看；一般是研究区或迹线边界的前后处理。 |
| **验证.py, 测试.py** | 验证/测试 | 小脚本，按需运行。 |

使用顺序：若有原始点数据 → 用 `traces.py` 生成迹线 geojson；再用 `area.py` 从迹线生成研究区 geojson；然后主流程用这两个文件。

---

### 2.5 快速对照：我想做…该用哪个？

| 你想做的事 | 用哪个 |
|-------------|--------|
| 点按钮就能看各种图、改参数选等值线 | **main.py**（先激活 envs/fract） |
| 把当前研究区导出成网格 CSV 做 GIS 分析 | **data export.py** |
| 只要一张「分类+拓扑」的 PDF | **分类后-拓扑后.py** |
| 对比两个区域的拓扑参数 | **1.py**（改两套数据路径） |
| 看多区域迹线长度分布和幂律 | **2.py** |
| 看拓扑验证错误长什么样 | **3.py** |
| 从栅格图/骨架做网格玫瑰图 | **new plot/new plot.py**（并备好 analyis、fatbox） |
| 原始点数据转迹线 GeoJSON | **数据处理/traces.py** |
| 从迹线生成矩形研究区 | **数据处理/area.py** |

---

## 三、总结

- **第一个「环境」= envs（尤其是 envs/fract）**：是 Python 虚拟环境，提供 fractopo、geopandas、PyQt5 等；**使用方式** = 先激活该环境，再在 `niuyuxi` 下运行相应脚本。
- **第二个「文件/项目」= niuyuxi**：是你的数据和脚本集合；**使用方式** = 按上面表格选对脚本，改好数据路径（必要时），在激活 envs/fract 后运行对应 `.py`。

若你告诉我「我当前只有一包坐标点」或「我只想改等值线网格大小」，可以再按你的情况写最小操作步骤（含命令和要改的变量名）。
