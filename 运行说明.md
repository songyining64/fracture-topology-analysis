# 怎么运行代码、要不要装新东西

## 一、先装依赖（第一次或换电脑时做一次）

在**项目根目录**（`断裂拓扑分析/`）下，用**本项目用的 Python 环境**执行：

```bash
cd /Users/mac/Documents/断裂拓扑分析
# 若用虚拟环境（如 venv_mac），先激活：
# source venv_mac/bin/activate   # Mac
# 或  envs\fract\Scripts\activate   # Windows

pip install -r requirements.txt
```

这会装上**主程序 + 国奖任务**需要的：fractopo、geopandas、PyQt5、matplotlib、scikit-learn、**xgboost、optuna、shap**、PyYAML 等。  
**不需要再单独装别的**，只要把 `requirements.txt` 装完，下面都能跑。

---

## 二、运行主界面（平时最常用）

```bash
cd /Users/mac/Documents/断裂拓扑分析/program
python main.py
```

会打开断裂拓扑分析的 GUI。若报缺包，回到上一步把 `pip install -r requirements.txt` 再跑一遍。

**数据源与默认加载：** 界面「数据源」下拉框对应 `program/THK/`、`KB11/`、`MY/` 下的迹线与研究区 GeoJSON。启动时会**自动选用第一个能加载成功的区域**（优先顺序：英买 MY → 柯坪 KB11 → 准噶尔 THK）。若 THK 缺少 `thkceshi-landmark1.geojson`，程序会跳过 THK 而不会卡在空数据；将该文件放到 `program/THK/` 后即可在界面中手动选「准噶尔盆地车莫古隆起」。

---

## 三、运行国奖相关脚本（特征工程、融合、训练、可解释性）

同样在 `program` 目录下，**依赖已用上面的 requirements 装好**，直接运行即可：

| 想做的事           | 命令 |
|--------------------|------|
| 导出网格 CSV       | `python export_grid_csv.py [THK\|KB11\|MY]` |
| 特征工程           | `python feature_engineering.py "Yingmai 2 area in Tarim Basin.csv"` |
| 加权融合           | `python fusion_algorithm.py "Yingmai 2 area in Tarim Basin.csv"` |
| 批量：特征工程+融合 | `python batch_run.py` 或 `python batch_run.py 区块1.csv 区块2.csv` |
| 训练 XGBoost       | `python ml/train.py` |
| 调参（Optuna）     | `python ml/tune.py` |
| 推理               | `python ml/infer.py` |
| SHAP 可解释性      | `python ml/explain.py` |
| 两区拓扑参数对比   | `python compare_topology_params.py` |
| 迹线长度幂律拟合   | `python trace_length_powerlaw.py` |
| 拓扑验证错误示意   | `python validation_errors_demo.py` |
| 分类+拓扑单图      | `python plot_classified_topology.py` |
| 网格采样测试       | `python grid_sampling_test.py` |

这些都会用到 **xgboost、optuna、shap**，已在 `requirements.txt` 里，**不用再额外安装**。

---

## 四、与 `requirements.txt` 的关系（高级能力）

当前仓库根目录的 **`requirements.txt` 已包含** `torch`、`umap-learn`、`torch-geometric`，与 GUI 里自编码器、VAE、UMAP、GAT 等高级按钮一致。  
一般只需 **`pip install -r requirements.txt`**，不必再按旧文档单独装上述包。若你手里是**自己裁剪过的精简版** `requirements.txt`，缺包时再按需补装即可。

---

## 五、总结

- **要装的新东西**：在项目根目录执行一次 **`pip install -r requirements.txt`** 即可，与 GUI 与国奖脚本所需一致。
- **运行主界面**：`cd program` → `python main.py`。
- **运行国奖脚本**：`cd program` 后按上面表格里的命令执行。

---

## 六、网盘打包给别人（解压后 `pip install` 就能用）

让对方拿到压缩包后：**解压 → 进入项目根目录（含 `requirements.txt` 的那一层）→ `pip install -r requirements.txt` → `cd program` → `python main.py`**。

### 建议**一定要带上**

| 内容 | 说明 |
|------|------|
| 项目源码 | 例如整份 `断裂拓扑分析/`：`program/`（`main.py`、`demo.py`、各 `.py` 脚本、`ml/`、`utils/` 等）、根目录 `requirements.txt`、本说明与其它 `.md` |
| `program/config.yaml` | 程序读取的配置 |
| 迹线与研究区数据 | `program/THK/`、`program/KB11/`、`program/MY/` 下**实际用到的** `.geojson`（以及你依赖的别名文件如 THK 的 `thkceshi-landmark1.geojson` 等）。缺文件时界面会跳过该区域或报错 |
| （推荐）已生成的网格 CSV | 若希望第三排「网格/相关性/三元图」等**开箱就有结果**：把各区域对应的 `*.csv`（如由 `export_grid_csv.py` 生成的**区块网格表**）一并放进 `program/` 或你界面里约定的路径。**不带**时，对方需自行运行 `python export_grid_csv.py THK|KB11|MY` 再在同一侧加载 |

### 建议**不要带上**（减小体积、避免路径混乱）

- 本机虚拟环境目录（如 `venv/`、`venv_mac/`、`envs/`）
- `__pycache__/`、`.pytest_cache/`、`.mypy_cache/`
- 编辑器与系统垃圾（`.DS_Store` 等）
- 体积很大的个人缓存；若不需要「免训练」，也可不带 `program/model/` 下已训好的权重（要带则整目录拷贝，并说明版本与数据是否一致）

### 可选（「更像成品」）

- `program/model/` 里已训练好的 XGBoost 等模型（路径需与 `config.yaml` / 代码一致）
- `program/data/processed/` 等中间结果（若有且对方也认路径）

**版本提示**：建议对方使用 **Python 3.9+**，与当前 fractopo / PyTorch 生态常见要求一致；Windows 上若装 `torch-geometric` 遇编译问题，可提醒按 PyG 官方说明用对应 wheel。
