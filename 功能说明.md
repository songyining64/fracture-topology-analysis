# 地质断裂拓扑智能分析系统 — 功能说明

本文按 **主界面从上到下、从左到右** 说明每一项的作用、**数据依赖**与**典型使用顺序**。程序入口：`program/main.py`。

---

## 0. 数据源（第一行）

| 项目 | 说明 |
|------|------|
| **数据源下拉框** | 在已配置的区域之间切换（如 THK 准噶尔车莫古隆起、MY 塔里木英买 2）。切换后会重新加载该区域的 **迹线 GeoJSON** 与 **研究区 GeoJSON**，用于 **上半部分所有基于地图/拓扑的绘图**。 |
| **与网格 CSV 的关系** | 「融合 / 机器学习 / SHAP」等按钮使用的是 **当前区域对应的网格 CSV**（如 `program/THK.csv`、`program/Yingmai 2 area in Tarim Basin.csv`）。若文件不存在，需先在 `program/` 下执行：`python export_grid_csv.py THK` 或 `MY`。 |

---

## 1. 第一排：基础地质与拓扑绘图（基于 GeoJSON）

下列功能均依赖 **当前数据源的迹线 + 研究区**；一般先选好数据源再点击。

| 按钮 | 功能简介 |
|------|----------|
| **原断裂数据地图** | 在研究区范围内绘制原始断裂迹线，用于检查数据范围与几何是否正常。 |
| **分类后数据地图** | 按 fractopo 对分支/节点类型的分类规则，在地图上以不同颜色绘制迹线分类结果。 |
| **断裂密度热力图** | 基于迹线与研究区做核密度或网格密度可视化，反映断裂空间集中程度。 |
| **方位角集图** | 展示迹线方位分布（与方位集/组有关的可视化，便于看优势走向）。 |
| **方向玫瑰图** | 迹线方位与分支方位的 **玫瑰图**（极坐标柱状），用于对比两组方位分布。 |
| **各类别三元图** | **节点类型（XYI）** 与 **分支类型（CC/CI/II 等）** 的两个三元（三角）图；展示类型占比及分位数参考线等。 |
| **交叉与相邻关系** | 对不同方位集合之间 **cross-cut（切割）** 与 **abutting（邻接）** 关系统计，多子图柱状图对比。 |
| **确定分支和节点** | 调用 fractopo 计算 **分支与节点**，**左右并排** 展示：迹线+研究区、节点散点+研究区；用于检查拓扑对象空间分布。 |
| **长度分布拟合** | 对迹线或分支长度做 **幂律（powerlaw）** 等分布拟合与诊断图（与 `powerlaw` 相关）。 |

**说明：** 本排不要求网格 CSV；要求对应目录下 **迹线 + 研究区** 文件齐全且坐标系一致（程序内会对地理坐标做 UTM 重投影以利于长度计算）。

---

## 2. 第二排：拓扑视图与参数成图

| 控件 | 功能简介 |
|------|----------|
| **拓扑视图下拉框** | 选择 **拓扑化后的断裂地图** 等视图类型；变更后会切换内部 `opt` 状态，与下一项配合使用。 |
| **绘制参数下拉框** | 选择要映射到颜色/等值线的 **拓扑或网格参数**（如断裂强度 B21/P21、迹线长度、节点数、分支长度等）。 |
| **显示拓扑属性数据** | 基于 fractopo 网络与 **轮廓网格 sampled_grid**，将所选参数绘成 **网格填色/等值线** 等拓扑属性图（需已成功构建 `Network` 且有有效网格几何）。 |

---

## 3. 第三排：属性融合与机器学习（基于网格 CSV）

本排多数功能依赖 **当前数据源对应的网格 CSV**（见第 0 节）。与第一排的「地图」是两条线：地图用 GeoJSON，本排用 **导出的表格**。

### 第三排界面导读（从左到右，与工具栏一致）

这一条工具栏可以理解为：**先看多种拓扑指标怎么“合成”视图 → 再训练模型 → 再解释模型**；其中前两个下拉/按钮（「融合方法」+「执行属性融合分析」）与后面的 **加权 / GAT / XGBoost** 在**算法上是不同路线**，不要混成同一个「融合」。

| 位置 | 控件名称 | 做什么 | 输出大致在哪 |
|------|-----------|--------|----------------|
| 1 | **融合方法**（PCA / 自编码器 / UMAP / VAE） | 仅决定「执行属性融合分析」时采用哪一种 **降维/非线性嵌入**，把多列拓扑属性压成 **2 个坐标**。 | 点击下一项后出 **二维散点图**（坐标轴为 PC1–PC2 或 Z1–Z2、U1–U2 等），颜色常为聚类。 |
| 2 | **执行属性融合分析** | 读取网格 CSV，取 `topology_fusion` 默认的一批**数值列**（强度、连通、长度等，见下文 §8.1），标准化后按左侧所选用 **PCA/AE/UMAP/VAE**，再 **KMeans** 聚类。 | 主图在右侧；不生成 XGBoost 模型。 |
| 3 | **高价值属性加权融合** | **不参与**上面的「融合方法」下拉。按专家规则给「连通率类」列更大权重，线性加权后 **全工区 min-max** 得到每格一个 **0–1 得分**。 | 主要在 **左侧文本**（均值、标准差、前若干行得分）。 |
| 4 | **融合对比(加权vsGAT)** | 同一批网格特征上，对比 **规则加权得分** 与 **GAT 图网络得分** 的分布（箱线图等）。GAT 需 **torch + torch_geometric**。 | 弹窗说明 + 箱线图；未装 PyG 时 GAT 侧常退化。 |
| 5 | **训练XGBoost模型** | 用特征工程后的矩阵预测 **某一目标列**（如断裂强度）；可弹窗输入列名；模型存到 CSV 同目录下 **`model/xgboost_reg.json`**。 | 训练曲线等图 + 左侧文字指标。 |
| 6 | **SHAP关注特征**（下拉框） | 列出 **与当前训练管道一致** 的特征名（随数据源刷新）。选「全部」则 SHAP 摘要图为默认顺序；选某一列则该特征在图中 **排到最前** 便于阅读。 | 影响 **SHAP** 出图与文字说明，不单独出图。 |
| 7 | **SHAP可解释分析** | 加载已保存的 XGBoost，用当前 CSV 重算 **SHAP**，解释「各特征如何把预测推高/拉低」。 | 左侧 **特征贡献表** + 右侧 **SHAP summary 图**；须先训练。 |
| 8 | **一键空间-拓扑融合** | 弹窗输入 **目标列** 后，自动串联：**特征工程 →（可选）加权 / 自适应 / GAT → 多尺度与图嵌入（按依赖）→ XGBoost → SHAP**，写 `data/processed` 等目录，耗时可能较长。 | 左侧长文本摘要 + 右侧 SHAP 等图。 |

**记忆口诀：** 「**左边下拉 + 执行融合**」= **看图聚类**；「**加权 / GAT / 一键**」里才带 **勘探打分或图网络**；「**训练 + SHAP**」= **有监督预测 + 解释**。

### 3.1 融合方法 + “执行属性融合分析”

| 控件 | 功能简介 |
|------|----------|
| **融合方法** | 在 **PCA / 自编码器 / UMAP / VAE** 中选择一种 **降维方式**（将多列拓扑指标压成 2 维主成分或隐变量）。AE、VAE 需 **PyTorch**；UMAP 需 **umap-learn**；PCA 仅 sklearn 即可。 |
| **执行属性融合分析** | 读取网格 CSV 中 `topology_fusion` 配置的 **默认拓扑属性列**，标准化后做所选降维，再 **KMeans 聚类**，输出 **PC1–PC2（或等价）散点图**，颜色多为 **cluster_id**。 |

**用途：** 多块网格在「综合拓扑特征空间」里的分群与可视化，**不**等同于「高价值属性加权融合」的打分规则。

### 3.2 高价值属性加权融合

| 按钮 | 功能简介 |
|------|----------|
| **高价值属性加权融合** | 在 `feature_engineering` 特征矩阵上，对 **连通率类** 等列施加更高权重，做线性加权并 **全工区 min-max 归一化** 到 [0,1]，得到每格 **融合得分**。结果主要显示在 **左侧文本**；**最低一档会显示为 0**，属归一化正常现象。 |

### 3.3 融合对比（加权 vs GAT）

| 按钮 | 功能简介 |
|------|----------|
| **融合对比(加权vsGAT)** | 在同一批网格特征上，对比 **规则加权融合** 与 **GAT 图注意力网络** 得分分布（箱线图等）。**GAT** 依赖 **torch + torch_geometric**；未安装时 GAT 一侧常为 0 或退化，程序会提示。 |

### 3.4 训练 XGBoost 模型

| 按钮 | 功能简介 |
|------|----------|
| **训练XGBoost模型** | 用 `feature_engineering.build_feature_matrix` 构建 **X、y**；若未能自动识别目标列，会弹窗让用户输入 **要预测的列名**（如 `Fracture Intensity B21`）。**5 折交叉验证 + 留出测试**，将模型保存到 `program/<区域>/model/xgboost_reg.json`（路径随 CSV 所在目录变化），并在界面展示训练曲线等。 |

### 3.5 SHAP 可解释分析

| 控件 | 功能简介 |
|------|----------|
| **SHAP关注特征** | 下拉列表列出 **当前 CSV 经特征工程后实际进入模型的特征名**。选 **「全部」** 时与常规 SHAP 摘要图一致；选 **某一列** 时，该特征在摘要图中 **排到最前**，便于查看。 |
| **SHAP可解释分析** | 加载已训练的 `xgboost_reg.json`，对当前数据重算 **SHAP**，生成 **特征贡献表**（左侧文字）与 **summary 图**（右侧）。**必须先训练 XGBoost**；否则会提示先训练。 |

### 3.6 一键空间-拓扑融合

| 按钮 | 功能简介 |
|------|----------|
| **一键空间-拓扑融合** | 弹窗输入 **目标列**，依次执行 **特征工程 → 加权/自适应/GAT（视依赖是否安装）→ 多尺度/图嵌入（若适用）→ XGBoost → SHAP** 的流水线，在 **data/processed** 等目录写结果，并在界面展示部分指标与 SHAP 图。运行时间可能较长。 |

---

## 4. 多图切换（画廊）

当一次操作产出 **多张图**（如玫瑰图两张、三元图两张）时，画布上方会出现 **「上一张 / 下一张」**，用于在同一批图中切换，无需缩小窗口。

---

## 5. 左侧文本区

用于输出 **运行日志、报错、数值表**（如加权融合统计、SHAP 表、空间-拓扑流水线指标等）。与右侧图 **可能来自不同按钮**（例如先加权再 SHAP），阅读时注意当前最后一次操作提示。

---

## 6. 命令行补充脚本（非主界面按钮）

| 脚本/模块 | 作用 |
|-----------|------|
| **`program/export_grid_csv.py`** | `python export_grid_csv.py THK` 或 `MY`：从迹线+研究区生成 **网格拓扑 CSV**，供融合与 ML 使用。 |
| **`program/run_comparison_experiment.py`** | 批量对比 **加权 vs GAT、多种基线与 XGBoost** 等实验并出图（适合写论文/报告复现）。 |
| **`program/spatial_topology_framework.py`** | 可作为模块被「一键」调用，也可在命令行单独跑流水线（见文件内 argparse）。 |
| **`program/config.yaml`（若存在）** | 高价值属性名、融合权重、训练折数等 **统一配置**。 |

---

## 7. 推荐阅读顺序（新手）

1. 选数据源 → **原断裂数据地图** 确认数据。  
2. **分类后数据地图** / **拓扑属性** 理解拓扑结果。  
3. 生成 **网格 CSV** → **执行属性融合（PCA）** 看低维聚类。  
4. **训练 XGBoost** → **SHAP**；需要 GAT 时再安装 **PyTorch Geometric** 后试 **融合对比** 或 **一键融合**。  

---

## 8. 各类融合方法：作用、区别与依赖

本节说明软件里出现的几种「融合」分别解决什么问题、输入输出是什么、和 **第三排第一个下拉** 的关系。

### 8.1 「执行属性融合分析」用的四类降维（由「融合方法」下拉选择）

它们共用同一批 **网格 CSV 数值列**（`topology_fusion` 默认约 14 列：多种 **断裂强度/无量纲强度/面频/连通/迹线与分支条数及长度** 等；CSV 里没有的列会自动跳过）。流程共性：**标准化 → 降到 2 维 → KMeans(默认 4 类) → 散点着色 = 簇号**。

| 方法 | 作用（直觉） | 特点 | 依赖 |
|------|----------------|------|------|
| **PCA** | 找出 **线性** 组合后方差最大的方向，得到 PC1、PC2；可理解为「综合差异最大的两个正交轴」。 | 速度快、可解释（载荷向量）、结果稳定；只能表达 **线性** 结构。 | `scikit-learn` |
| **自编码器（AE）** | 用 **神经网络** 把多维多压到 2 维隐层，再重构回原空间；最小化重构误差。 | 可表达 **非线性** 关系；隐空间不一定正交。 | **PyTorch** |
| **UMAP** | 在 **流形** 上保持距离结构，强调「局部邻域」相似性，常用于可视化聚类团块。 | 簇往往更「抱团」；对超参略敏感；**非线性**。 | **umap-learn** |
| **VAE** | **变分** 自编码器：隐变量带概率分布，生成的 latent 更平滑，适合需要「概率式」隐空间的场景。 | 训练比 AE 重；同为非线性。 | **PyTorch** |

**用途归纳：** 这四种都是 **无监督地把「多指标表格」变成平面上的点 + 分簇**，便于看「哪些网格在综合拓扑意义上相近」；**不直接给出**「勘探价值打分」，也 **不训练 XGBoost**。

### 8.2 高价值属性加权融合（规则加权）

- **作用：** 按配置把 **连通率相关列** 权重大于其他列，做 **加权求和**，再对全工区做一次 **min-max** 得到 **0–1 得分**。  
- **与 PCA 等的区别：** 不做降维，而是 **人为规则 + 标量得分**；每格一个数，便于排序、与阈值对比。  
- **依赖：** `feature_engineering` + `fusion_algorithm`，无需 PyTorch。

### 8.3 GAT 融合（图注意力网络）

- **作用：** 把规则网格看成 **图**（格与格邻接），用 **图注意力** 聚合邻居信息，得到每个节点的 **融合型得分**（与规则加权不同路径）。  
- **与加权对比：** 权重视 **数据与邻域结构** 学习（在依赖齐全时），不是固定专家权重。  
- **依赖：** **PyTorch** + **torch_geometric**；未安装时程序会降级或提示，对比图里 GAT 一侧可能接近 0。

### 8.4 一键空间-拓扑融合里的「融合」

这里的「融合」是 **流水线名词**：在 `spatial_topology_framework` 中通常包括 **特征工程后的规则加权、（可选）自适应加权、（可选）GAT、（可选）GraphSAGE 嵌入、多尺度特征** 等，再接 **XGBoost 预测指定目标列** 和 **SHAP**。  
适合：**已经明确预测对象**（某列物理/拓扑量），希望 **一次跑完** 从特征到模型到解释的全过程。

### 8.5 如何选用（简要）

| 你的目的 | 更合适的入口 |
|-----------|----------------|
| 只想看多维拓扑指标在低维平面上怎么分群 | **融合方法** 选 PCA/UMAP/AE/VAE → **执行属性融合分析** |
| 想要每格一个「综合打分」且偏专家经验 | **高价值属性加权融合** |
| 想对比传统加权与图学习 | **融合对比(加权vsGAT)**（装好 PyG） |
| 要预测某一列并看特征贡献 | **训练 XGBoost** → **SHAP**（可用「SHAP关注特征」定位某一列） |
| 一次跑整条论文级流水线 | **一键空间-拓扑融合** |

---

*文档版本与界面一致，以 `program/main.py`、`program/demo.py` 为准；若后续增加按钮，请同步更新本说明。*
