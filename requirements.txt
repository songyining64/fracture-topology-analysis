# 油气区断裂网络连通性智能分析与预测系统 — Python 依赖（项目根目录执行 pip install -r requirements.txt）
fractopo>=0.6.0
geopandas>=0.12.0
PyQt5>=5.15
matplotlib>=3.5
scipy>=1.9
pandas>=1.4
numpy>=1.21
scikit-learn>=1.0
PyYAML>=6.0
ternary
powerlaw
pyshp
scikit-image
opencv-python
networkx

# 任务1&2：智能融合与 ML（国奖闭环）
xgboost>=1.6
optuna>=3.0
shap>=0.42

# 高级能力（与 GUI 全部一致：自编码器 / VAE / UMAP / GAT / 融合对比 / 一键流水线）
# 缺下列包时对应按钮会报错或降级，请一并安装。
torch>=2.0.0
umap-learn>=0.5.0
torch-geometric>=2.4.0
